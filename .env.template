# GitHub API Token (optional but recommended to avoid rate limits)
# Get yours here: https://github.com/settings/tokens
GITHUB_TOKEN=your_github_token_here

# Put real keys in a .env file, do not commit .env to version control
